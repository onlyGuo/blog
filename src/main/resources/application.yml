server:
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=UTC
  freemarker:
    cache: false
    charset: utf-8
    suffix: .ftl
